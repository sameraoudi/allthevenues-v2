# Deny direct web access. View templates + handlers are included by the front
# controller (index.php) via the filesystem — never fetched as URLs.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
